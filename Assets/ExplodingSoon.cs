using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExplodingSoon : MonoBehaviour
{
/*    private float amp = 1f; // amplitude
    private float speed = 1f;
    private float pos = Mathf.Max(0f, Mathf.Sin(Time.time * speed) * amp);*/
}
